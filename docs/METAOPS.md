# CakeCAD 元操作研究（METAOPS）

> 层级：L1 研究文档 · 状态：研究中（结论成熟后回写 `MASTER_SPEC.md` B.4 / C.13）  
> 动机：现有命令清单（B.4.3，16 个）是一拍脑袋的平面列表；菜单（B.4.5）有注册表没有的动词；内核事件（C.13，17 种）与命令从未对齐。必然存在"大操作是小操作的组合"，也必然撞车、不正交。  
> 方法纪律：**不预设正交性为目标。** 判断标准只有两个——事实（它到底往事实底座写了什么）与实践（工作流频率、心智模型、承诺级别）。重合不等于错误，但重合必须被看见、被命名、被裁决。
>
> 阶段纪律：项目仍在 PRD/TRD 阶段，需求本身还在变。本文件的 F 原语、内核事件名、plane 字段等全部是**概念层记账工具**（用来检查和裁决，不是用来实现），禁止把任何条目当作函数签名、类名或数据结构定稿。

---

## 1. 三个层面

操作体系不是一张平面表，而是三层：

```text
L-cmd    用户命令     人看到的动词：倒入、混合、分件……（B.4.3，16 个）
L-kernel 内核事件     事件日志里的类型：transfer、combine、process_start……（C.13，17 种）
L-fx     效果原语     对事实底座的最小突变：对象出生、关系开闭、标签挂载……（本文件首次定义）
```

约束方向：

- **效果原语必须可数、可撤销**——撤销系统（L017）最终建立在它上面；
- **内核事件尽量小且互斥**——同一类事实变化只应有一个事件类型能写它，否则事件日志失去唯一真相的地位；
- **用户命令允许重叠、允许是宏**——命令为工作流服务，不为代数 purity 服务；但同一内核写入不得因入口不同而不同（一致性底线）。

## 2. 效果原语清单（L-fx）

对事实底座的所有可能突变，穷举如下。任何命令、任何内核事件，最终都只能由这些原语组合而成：

| 编号 | 原语 | 说明 |
|---|---|---|
| F1 | 对象出生 | 创建新身份（含过程中的暂定产物） |
| F2 | 对象退役 | 结束存在区间（消耗/废弃；不是删除，历史保留） |
| F3 | 关系开区间 | located_in / derived_from / conforms_to 等生效 |
| F4 | 关系闭区间 | 上述关系失效（记录终点） |
| F5 | 标签挂载/摘除 | "正在消失于 P""正在被创建于 P"等稀疏标签 |
| F6 | 值写入 | ValueCell：量、参数、状态字段 |
| F7 | 锁点产生 | 实测/手动等 Lock，切断自动传播 |
| F8 | 量分配 | 质量/数量在对象间记账（转移、拆分、损耗登记） |
| F9 | 时间区间开 | 过程区间左端 |
| F10 | 时间区间闭 | 过程区间右端 |
| F11 | 名称写入 | 命名/改名 |
| F12 | 观察/注释记录 | observe、锚点、无承诺信息 |
| F13 | 绑定/解绑 | 来源、配方、变量绑定的建立与解除 |
| F14 | Recipe 结构变更 | 版本分叉、diff/merge（**不在 Run 平面**） |
| F15 | 注册表写入 | MethodDef、命令、模型胶囊登记 |
| F16 | 分析实体创建 | Question / AnalysisCase / Result |
| F17 | 文件导出 | exports/ 写文件 |
| F0 | 零写入 | 纯视图操作 |

## 3. 内核事件 → 效果原语

C.13 的 17 种内核事件逐个拆成原语，验证内核是否互斥：

| 内核事件 | 原语组合 | 互斥性检查 |
|---|---|---|
| create | F1 | 干净 |
| transfer | F8 + F3/F4（位置关系迁移）+ F6（量记录） | 干净 |
| combine | F9 + n×transfer + F5 + F1（暂定产物） | ⚠ 内含 transfer，见 §5-C3 |
| separate | F1×N + F13（derived_from）+ F8（分配） | 干净 |
| place | F3/F4（仅 located_in，不动账） | 与 transfer 的边界见 §5-C4 |
| process_start | F9 | 与 combine 撞，见 §5-C3 |
| process_end | F10 + F5 摘除 + F1/F2 状态迁移 | 干净 |
| pause / resume | F5（过程状态标签） | 干净，但尚无命令 |
| intervene | F12 + F6（过程内记录/参数变更） | 干净，但尚无命令 |
| abort | F10 + F2/F5（中止语义迁移） | 干净，但尚无命令 |
| measure | F6 + F7 | 干净 |
| observe | F12 | 干净 |
| annotate | F12（带锚点） | ⚠ 与 observe 边界模糊，见 §5-C6 |
| name | F11 | 干净 |
| bind / unbind | F13 | 干净，但尚无命令 |

**结论 A**：内核大体互斥，真正的问题集中在三处：combine 与 process_start 的包含关系、place 与 transfer 的边界、observe 与 annotate 的重合。

## 4. 用户命令 → 内核事件分解

B.4.3 全部 16 个命令逐个分解，并标注所在平面：

| 命令 | 内核分解 | 平面 |
|---|---|---|
| new 新建对象 | create（+place，若拖入时指定位置） | Run |
| name 命名 | name | Run |
| place 放入 | place | Run（declared） |
| pour 倒入 | transfer + place | Run（committed） |
| takeout 取出 | transfer（+create，若取出物需独立身份） | Run（committed） |
| mix 混合 | combine（= process_start + n×transfer + 双向缓冲标签） | Run（committed） |
| split 分件 | separate | Run（committed） |
| start 开始操作 | process_start | Run（committed） |
| end 结束操作 | process_end | Run |
| measure 记录测量 | measure | Run（measured） |
| record 回填实测 | measure（目标=过程/Run） | Run（measured） |
| ask 提问 | Question → AnalysisCase（F16） | Analysis |
| fork 新建版本 | Recipe 分叉（F14） | Recipe |
| lanes 召唤泳道 | 无（F0） | 视图 |
| export 导出手册 | 无事实写入（F17） | 文件 |
| defmethod 新建方法 | MethodDef 登记（F15） | 注册表 |

**结论 B（重要）**：16 个命令横跨五个平面（Run / Recipe / 注册表 / Analysis / 视图+文件）。命令注册表必须为每个命令声明 `plane` 字段，否则"撤销"这个词对不同命令意味着完全不同的东西——撤销 pour 是回退事件，撤销 fork 是删除未引用版本，撤销 lanes 根本没有意义。这是旧命令清单完全没处理的维度。

**结论 C**：mix 是唯一的真复合命令：一次 mix = 1 个 process_start + n 个 transfer + 标签挂载 + 暂定产物出生。中途加黄油就是区间内再发一条独立 transfer——所以"打面中途加料"不需要新命令，pour 天然支持（目标=进行中的过程）。

## 5. 撞车矩阵与逐对裁决

以下每对都写入相同或高度重叠的内核事件。逐对给出：事实差异（写的东西差在哪）、实践差异（用起来差在哪）、裁决草案。

### C1：pour 倒入 ↔ takeout 取出

- **事实**：同为 transfer。方向相反；takeout 可能多一个 create（取出的那一份要不要变成独立对象）。
- **实践**：倒入是生产主流（目标=容器/过程）；取出是熵减/纠错/取样主流（目标=台面/新 Pile/废弃）。
- **草案**：内核合并为单一 transfer(from, to, 量)；命令层保留两个动词——提示语、默认目标、角色过滤器都不同，合并成一个"转移"动词会逼用户每次额外指定方向。**允许不正交，理由=心智模型不同。**

### C2：record 回填实测 ↔ measure 记录测量

- **事实**：完全同内核（measure），仅宾语类型不同（对象 vs 过程/Run）。
- **实践**：用户在曲奇流程里会在对象上测（面团 80g）也会在过程上回填（烘烤实际 13min）。
- **草案**：**合并为 measure**，签名目标槽位放宽为 object|process|run。少一个动词，无信息损失。待用户确认。

### C3：mix 混合 ↔ start 开始操作

- **事实**：mix = start(type=combine) + n×transfer + 双向缓冲。start 是 mix 的真子集。
- **实践**：混合是最高频过程且入口物料必须在启动瞬间动账，值得一个一键动词；通用 start（烘烤、冷藏、静置）不需要立即动账。
- **草案**：**保留 mix 作为 start 的便利封装**，但规格必须写明：mix 不产生任何 start 表达不了的事实，二者共享同一 process_start 内核。combine 不再作为独立内核事件，降级为 process_start 的 type 参数 + 自动伴随 transfer——这同时解决 §3 里 combine 与 process_start 的互斥性问题。**这是"大操作=小操作组合"的第一个正式实例。**

### C4：place 放入 ↔ pour 倒入

- **事实**：place 只写 located_in（declared，不动账）；pour 写 transfer+located_in+量（committed，动账）。pour ⊃ place 的空间效果。
- **实践**："把碗放进烤箱"与"把面粉倒进碗"在承诺级别上完全不同——前者是摆位，后者是物料账本变更。若不区分，用户每次摆位都被迫声明量，或每次倒料都逃账。
- **草案**：**保留两个动词，边界用承诺级别写死**：place=declared/不动账；pour=committed/动账。角色过滤器辅助：pour 的主语必须是 material。**允许语义重叠，理由=承诺级别不同；这正是"操作自由，仿真诚实"在命令层的体现。**

### C5：takeout 取出一部分 ↔ split 分件

- **事实**：从一团面取 50g，和把面团分成 6 份，都是量的切分；区别在 separate 创建 N 个带来源关系的子对象（身份），而 takeout 的量可以没有身份（倒进垃圾桶不需要传记）。
- **实践**：取出的东西如果需要被后续追踪（取样送检、留种），它就是对象；如果不需要（丢弃、试吃），它只是账本数字。
- **草案**：边界规则=**身份需求**。takeout 默认不创建对象，选项 `--as-object` 升级为 separate 的 1:1 特例。split 保持 N 路等分/指定分配。**这条规则同时回答"取出来的算不算新对象"这个旧坑。**

### C6：observe 观察 ↔ annotate 注释

- **事实**：同为 F12，区别只在是否带对象锚点。
- **草案**：**内核合并为 observe（可带锚点列表）**，annotate 降级为别名。"插入书签到 MD"命令写的就是 observe+锚点。

### C7：new 新建 ↔ 过程产物出生

- **事实**：两条对象出生通道——用户显式 create，与 process_end/combine 内部的产物 create。
- **实践**：前者是用户主动引入，后者是过程推导（临时名可后改）。
- **草案**：内核统一为 create（source 字段区分 manual / process_output）。无需新命令，但 Inspector 传记必须能区分两种出生。

### C8：菜单孤儿（B.4.5 有、B.4.3 没有的动词）

| 菜单动词 | 对应内核 | 处置草案 |
|---|---|---|
| 暂停 / 介入 / 中止 | pause / intervene / abort | **补正式命令**（过程区右键）。3D 打印换色、中途翻面团都需要 |
| 恢复（resume） | resume | 内核有但菜单漏了，一并补 |
| 绑定来源 | bind / unbind | **补正式命令**（多选区）。5g 精油后补来源靠它 |
| 装配 | combine 子类型 assemble | **暂缓**，作为 MethodDef 或 start(type=assemble) 处理，不单立动词 |
| 清空 | transfer（全部→废弃）+ F4 | 草案：takeout --全部 --to 废弃 的别名，不单立 |
| 标记已清洗 | observe + 状态标签（F5/F6） | 草案：name/observe 类轻命令，可补 `mark` 通用标记命令 |
| 设定参数 | F6（设备 ValueCell） | 草案：补 `set` 命令（object × 参数 × 值） |
| 查看档案 / 插入书签 / 撤下带入热区 | F0 / F12 / F0 | 视图与导航动作，**不进命令注册表**，归为 UI 动作层 |
| 移动 move | 画布坐标 | **关键裁决**：坐标是纯视图状态还是事实？草案=视图状态不进事件日志（除非用户把位置提升为 Region/关系），move 不做命令，拖拽与 CMD 坐标输入直接写视图状态 |

### C9：undo / redo

- 每个命令都声明了"撤销"，但没有 undo 命令本身。
- **事实问题**：Run 是 append-only，撤销 pour 不能删事件，只能发**补偿事件**（compensating event）或在 Recipe 层做真删除。两个平面的撤销语义不同。
- **草案**：undo/redo 进注册表，plane=meta；Run 平面撤销=追加补偿事件（账本风格），Recipe 平面撤销=真回退（未执行的设计无账本义务）。**这同时是 L017（撤销/历史树）的第一个正式结论。**

## 6. 组合律：大操作如何由小操作合成

从曲奇与 chat 资料里的真实流程归纳：

```text
打面      = mix + 0..n×pour(中途) + end
烘烤      = start(type=bake, 设备) + … + end + record(实测)
分团整形  = split(N) + 0..n×place
取样送检  = takeout(--as-object) + name + place(送检区)
清洗归位  = takeout(残余) + mark(已清洗) + place(归位)
腌制/冷藏 = start(type=marinate/chill) + end（无动账，纯时间区间）
```

**推论 1**：用户级"大动词"（烘烤、腌制、水浴……）**不应进命令注册表**——它们是 start 的 type 参数，或用户自定义 MethodDef 的模板。注册表保持小（约 20 个），方法库无限生长。这把"阿姆斯特朗烤箱"式自定义和通用命令的边界正式画出来了。

**推论 2**：复合的唯一合法机制是**命令发出一串内核事件**，不存在命令调命令。这保证 CMD 内联、右键、工具栏三个入口行为一致，也保证撤销可以按事件粒度回退。

## 7. 非正交性政策（草案）

1. 效果原语层：可数、可撤销，变化不重叠。
2. 内核事件层：互斥优先；发现的非互斥（combine）降级为参数化。
3. 用户命令层：**允许重叠**，但每处重叠必须在本文件登记（§5），且写明实践理由；无理由的重合视为事故。
4. 同一内核写入不得因入口（右键/工具栏/CMD）不同而不同。
5. 菜单与注册表必须一一对应：菜单里出现的每个动词要么在注册表、要么明确属于 UI 动作层（F0/F12 类）。

## 8. 裁决清单（待用户逐条拍板）

| # | 议题 | 草案 |
|---|---|---|
| D1 | record 并入 measure | 合并 |
| D2 | combine 内核降级为 process_start 的 type | 降级 |
| D3 | pour/takeout 保留两个动词，内核共用 transfer | 保留 |
| D4 | takeout ↔ split 边界=身份需求，加 `--as-object` | 采纳 |
| D5 | observe/annotate 内核合并 | 合并 |
| D6 | 补命令：pause / intervene / resume / abort / bind / set / mark | 补 |
| D7 | 清空=别名不补命令；装配=MethodDef 不单立 | 采纳 |
| D8 | 移动坐标=视图状态，不进事件日志 | 采纳 |
| D9 | undo/redo 进注册表，Run 用补偿事件，Recipe 用真回退 | 采纳 |
| D10 | 大动词（烘烤/腌制…）不进注册表，走 start type / MethodDef | 采纳 |
| D11 | 命令注册表增加 plane 字段（run/recipe/registry/analysis/view/file/meta） | 采纳 |

## 9. 裁决后的命令注册表目标态（预览）

若 D1–D11 全部通过，注册表为约 20 个命令：

```text
Run 平面：new name place pour takeout mix split start end
          measure pause intervene resume abort bind unbind set mark
Recipe 平面：fork
注册表平面：defmethod
Analysis 平面：ask
meta：undo redo
视图/文件（UI 动作层，不入注册表）：lanes export 档案 书签 热区 移动
```

与现行 B.4.3 的差异：+pause/intervene/resume/abort/bind/unbind/set/mark/undo/redo，−record，lanes/export 移出注册表。

---

## 10. 不可穷举操作：能力声明层（火鸡情景研究）

来源：用户提出的火鸡情景（L046）——右键火鸡，新建或复用一个动作"掰下它的右侧 drumstick"；更有甚者烤到一半"打开烤箱并掰下右侧 drumstick"。同类：芒果塔派盘皮修边、火鸡塞料。这是**难以提前穷举预测的高自由度操作**的样本。

### 10.1 先验证：事实层用现有原语能否表达

"烤一半开门掰右腿"全部分解为已有内核：

```text
intervene(烘烤过程 P)        ← 开门，过程内介入事件（内核已有 pause/intervene/resume）
split(火鸡, part=右drumstick) ← separate 内核，子对象带来源关系
  （取出则再 takeout；留在烤箱则无后续）
resume(P)
```

结论：**操作层没有缺口**。真正的难点只有一个——"右侧 drumstick"这个部分从哪来？

### 10.2 命名部件目录（named parts）

草案：对象可以携带一份**部件声明**——火鸡的官方能力包声明"有四肢（左腿/右腿/左翅/右翅）""能塞入（腹腔）""能被切"。关键规则：

- 部件被操作前**只是声明的潜在分界**，不是对象，不产生账本；
- 被掰下/切下那一刻才实体化为子对象（F1 + derived_from + from_part=右腿）；
- 这是懒身份：与懒语义、懒仿真、懒几何同族的第四条懒原则候选。

### 10.3 "能从中间劈开的"是什么：能力声明（affordance）

逐条排除：

- **不是过程**——过程是已发生或正在发生的事，能力声明什么都没发生；
- **不完全是 MethodDef**——MethodDef 是"怎么测、怎么算"的用户方法（带指标）；能力声明是"能被怎么操作"；
- **不是普通标签**——它带宾语签名和后果模板，是可被调用的。

草案定义：**能力声明 = 谓词 + 宾语签名 + 默认后果模板**，挂在对象或对象类上，是注册表数据。调用它产生的动作才是过程/事件。

后果自定义（用户原话的落地）：调用时后果 = 模板默认（split + 质量分配 + 标签）或当场编辑覆盖，走 Case Preview 式确认。后果由原语组合：F1 子对象出生、F5 标签（如"烤过"）、F8 量分配。没有后果也合法——纯记录动作发生。

官方与用户同构（用户明确要求）：能力包是同一注册表里的数据，官方"带有四肢的火鸡"和用户当场加的"能从中间劈开"机制完全同一条；用户能力包可存库。这把 §6 推论 1 再推进一层：不只方法库可生长，**对象能力库也可生长**，二者都是注册表数据而非代码。

### 10.4 空间语义的承载：标签层 vs 网格层

用户提出的两条路线是**并列的本体方案**（物料与性质逻辑住在哪里）：

```text
路线 A（网格本体）：对象=无碰撞布料/海绵网格，物料逻辑住在几何里——
                    节点带 voxel 与 mass，切割时现场重算节点与边拓扑，
                    切下来的部分的性质由网格分配继承
路线 B（标签本体）：便签+自然语言声明性质；图像仅展示（贴图/有限展示建模，
                    如月饼剖面只在末端画出）；后果当场自定义；能力库预置常见物料
```

关键推论（用户点明）：**若标签是本体，几何层被彻底解放。** 塔皮"能软贴合派盘、边缘能被割掉、割下来的边有自己的质量和继承参数"这类物料与性质逻辑全部由标签+自然语言承担，3D 不必为它们付任何代价——可以用非布料的普通 3D，建死物，甚至不建，它只是个草稿。

这消解了路线 A 的成本结构：网格方案的昂贵恰恰来自"它要当本体"（切割重算、节点质量、边拓扑维护）；本体让给标签之后，这套 machinery 整个不需要存在。

#### 10.4.1 双方案全面对比

| 维度 | 标签本体 | 网格本体 |
|---|---|---|
| 表达能力 | 离散符号：事件、账本、命名部件、自然语言。表达"左边焦了 2cm 一条带"笨拙 | 连续场：空间分布（温度场、厚度、偏料）天然一等 |
| 演化自由度 | 加新性质=加标签，零成本；schema 随时生长。**需求仍在变，此项权重最高** | 每类新物理行为可能要新节点属性/求解器/拓扑操作，schema 长在数据结构里，改动昂贵 |
| 诚实性 | 账目靠声明，可以错；但"未知合法""不守恒账本"本来就是核心设计，缺口由分析层指出 | 质量守恒由几何保证，切割自动分配——账目不会撒谎；**但代价是未知不被允许**，与"未知是合法状态"直接冲突 |
| 实现成本 | 本体层零图形学；图形预算全花在展示层 | 切割重算、拓扑、3D 拾取交互、渲染、性能全部进 critical path；单人+AI 工具链风险极高 |
| 体系兼容性 | 与事件日志、append-only、懒语义/懒仿真/懒身份、双向缓冲同族——全是离散符号系统 | 连续场系统与事件日志世界观冲突：每切一刀网格状态怎么 append-only？快照版本化与 diff/merge 复杂度爆炸 |
| 同步风险 | 单一事实底座 | 双本体=两套事实系统互相同步——与已废止的"全局 DAW 双向同步"同构的陷阱，更深 |

#### 10.4.2 对网格本体价值的诚实评估

网格本体真正不可替代的场景：切割/变形过程的**物理真实模拟**（拉面延展、裱花形变、翻糖贴合）。但 CakeCAD 的定位是研发过程的记录+分析（CAD/PLM/CAE），不是物理解算器；这类问题在懒仿真原则下本就"不提问就不算"。

关键缓解：**网格活在算例里，不活在本体里。** 真需要连续场时，Analysis 算例内部可以用 FEM/网格做临时内部表示，算例输出以区间/值/标签形式回到事实底座。本体层永远不需要网格。

标签本体失去的东西（诚实列出）：连续空间分布的直接表达（用命名部件/Region 离散替代）；自动质量守恒（用账本+多种未知/损耗条目替代）；切割的物理正确性（用算例按需补）。

#### 10.4.3 用户已表态的几何层定位

用户倾向（2026-09-02，待随本体裁决一并确认）：**几何层只作草稿与演示**——可以孤立存在，或被约束于一个标签作其下游可视化，**不直接参与物料流**；理由：3D 改物料逻辑的自由度不如标签灵活。此定位与已拍板的"视图是廉价投影"同构。

#### 10.4.4 裁决（已拍板，2026-09-02）

- **本体 = 标签层**（对象 + 稀疏标签 + 自然语言 + 命名部件）；
- 布料网不作为本体保留；网格只允许以算例内部临时表示存在；
- 几何层 = 草稿/演示，孤立或作为标签的下游可视化，不参与物料流。

已回写 MASTER_SPEC B.6。

### 10.5 开放问题（新增，待裁决）

| # | 议题 |
|---|---|
| O1 | 能力声明与 MethodDef 是否共享同一注册机制，还是两种实体 |
| O2 | 命名部件的概念身份：是对象内的 Region、子对象潜质声明，还是独立实体类型 |
| O3 | split-by-part 的默认量分配：模板声明 / 当场输入 / 未知合法 |
| O4 | 过程进行中对其参与者操作（烤到一半掰腿）：intervene 是否自动 pause，还是过程照常继续只记介入事件 |
| O5 | 能力包的作用域与存库规则（项目内 / 全局库），概念层即可，格式不定 |

---

## 11. 四个 bench 全程走查（命令体系的实战检验）

方法：每个流程走两遍——用 CakeCAD 逐帧走（只用已定义的概念：对象/标签/命名部件/能力声明/事件日志/命令），再走一遍"不用工具、纯手动跟进"，痛点对照。视角声明：§5 是内核撞车视角（自下而上），本节是工作流视角（自上而下），两者互补不重复。

### 11.1 Bench A：烤曲奇全程

**用 CakeCAD：**

```text
1  新项目。MD 随手写配方笔记（乱写合法，不是事实）
2  从对象库拖出：面粉/糖/黄油/蛋/巧克力豆/搅拌碗/厨师机/烤盘/烤箱
   → new+place（sketch，不动账）
3  measure(面粉, 质量, 120g) → 值单元+锁点（以后分析引用它）
4  多选面粉/糖/蛋 → pour → 碗（committed，账本：碗内+质量）
5  mix(碗内物料, 厨师机) → 过程区间开；双向缓冲：
   原料标"正在消失于混合"，mixture_01 标"正在被创建"
6  拖 playhead 到中途 → pour(黄油 → 进行中的过程) → 区间内嵌套 transfer
7  end → mixture_01 转为"已创建"；name → "曲奇面团"
8  split(面团, 12) → 12 个子对象+derived_from；默认均分，
   除不尽的进账本"未分配质量"条目（不强行守恒）
9  place(12团 → 烤盘)；place(烤盘 → 烤箱)；set(烤箱, 温度, 175°C)
10 ask "13 分钟够吗" → Case Preview（用了：面团质量/炉温；假设：经验
    传热模型；忽略：湿度）→ 结果卡：11–14min，置信 Supported
11 start(type=bake, 烤箱) → 开区间落在 playhead
12 中途开门看一眼：intervene(bake) + observe("上色均匀") → 过程照跑，
    介入是区间内嵌套事件（O4 的活例：不自动 pause）
13 end(bake) → record(烘烤, 实际 13min) → measured+锁点 →
    第 10 步的先验结果卡自动标 stale（依赖变了）
14 export(本次 Run) → 操作手册 MD
```

**不用 CakeCAD（手动）：** 配方记在备忘录，称重拍照；"13 分钟够吗"靠经验或搜索；烤完尝一口；下次想复现只能翻照片；用掉 20g 糖剩 80g 靠脑子记；改配方无版本只能手抄；导出手册=事后凭记忆重打一遍。核心痛点：**as-built 记录缺失**（做了但没留痕）、无法归因（这炉为什么苦）、分析（够不够烤）与记录（烤了多久）分居两个工具互不认识。

**本 bench 暴露：** 无新缺口；验证了双向缓冲、锁点、stale 传播、intervene-不-pause 的必要性。

### 11.2 Bench B：芒果塔（贴合派盘 + 刀修边）

**用 CakeCAD：**

```text
1  对象：塔皮面团、派盘（带命名部件"盘沿"）、擀面杖、刀（能力声明：
   能切）、芒果、卡仕达酱
2  start(type=擀制) → end；塔皮标签："已擀开，厚约 3mm"（自然语言+值）
3  place(塔皮 → 派盘) declared
4  贴合：start(type=贴合) → end；写入事实：塔皮与派盘的 conforms_to
   关系（形状随盘）+标签"已贴合盘形"。几何层可选画个草稿，不建也行
5  修边：split(塔皮, part=盘沿外余量) ← 命名部件提供分界；
   余料子对象出生（derived_from=塔皮），质量当场输入或标未知；
   塔皮账本减重或记"未分配"
6  余料去向：takeout/丢弃/回揉——回揉=pour(余料 → 新面团)账本回流
7  pour(卡仕达 → 塔壳)；split(芒果, 8) → place(芒果块 → 塔面)
8  ask "塔皮会烤缩吗" → 先验算例；烤完 record 实际收缩 → 后验对照
9  start(type=chill, 开放结束) → 不关它就一直开着，第二天 end
```

**不用 CakeCAD：** 修掉的边角料去向无人记（吃掉/丢弃/回揉？账就断了）；"贴合"这种形态事实纸面无法记录只能拍照；层结构比例（皮/馅/果）复现靠手感；跨次对比（这次边留多厚）无数据。

**本 bench 暴露：** ①命名部件在真实流程里被高频使用（盘沿、边缘余量），O2 必须尽快定；②修边的量分配（O3）在实操中大多是"未知合法+事后补称"；③conforms_to（形状随盘）这种"形态事实"用标签表达完全够用，验证了本体裁决。

### 11.3 Bench C：火鸡（塞料 + 烤一半掰右腿）

**用 CakeCAD：**

```text
1  对象：火鸡（官方能力包：有四肢[左腿/右腿/左翅/右翅]、能被塞入
   [腹腔]、能被劈开[用户当场加，存库]）、填料、烤箱、探针温度计、棉绳
2  measure(火鸡, 质量, 4.2kg) → 锁点
3  塞料：pour(填料 → 火鸡.腹腔) ← 宾语是命名部件！contained_in 关系
   建立。这就是"3D 空间承载语义"的标签层平替：不需要 3D 腹腔模型
4  mark(火鸡, "已缝合") 或 start(type=缝合)+end
5  set(烤箱, 180°C)；start(type=roast, 火鸡, 烤箱)
6  烤 40min 后 ask "中心温度到了吗" → Case Preview（用了：质量/炉温/
   已烤时长；没测过中心温 → Unknown 合法，结果给区间 60–68°C）
7  开门掰右腿：intervene(roast) → split(火鸡, part=右drumstick) →
   子对象"右腿"出生，**继承此刻状态标签**（腌过、烤过 40min），
   质量当场输入或未知 → takeout(右腿) → resume(roast)
8  继续烤 → end → record(出炉中心温, 74°C) measured
9  export → 手册："烤 40 分钟后取下右腿，余部继续烤至中心 74°C"
```

**不用 CakeCAD：** 掰下来的腿烤了多久、继承了主火鸡哪些处理（腌过、烤过40min）全靠脑记；塞了多少填料事后想不起；手册靠事后回忆重写，而回忆会美化。

**本 bench 暴露：** ①**参数/标签继承**是 split-by-part 的核心难点：子对象继承哪些标签（腔过、烤过）、不继承哪些（"完整"），需要继承规则草案；②能力声明的宾语可以是命名部件（火鸡.腹腔），签名体系要把"部件"纳入宾语类型；③intervene 不自动 pause 在此 bench 再次验证（开门 30 秒，烘烤事实未中断）。

### 11.4 Bench D：酸包（长周期 + 反复算 + 中途干预）

最重的 bench：酵种跨天跨项目、长时间发酵、途中把头伸进料理机、反复计算。

**用 CakeCAD：**

```text
1  酵种是跨项目对象（全局对象库）：上周喂过什么、活力历史都在它的传记里
2  Recipe：面粉 500g、水 350g、酵种 100g、盐 10g；水合率=变量表达式
   （绑定：水/面粉），改任一量自动重算
3  喂酵种（前一天）：pour(面粉+水 → 酵种罐)；start(type=ferment) →
   measure(酵种, 体积倍数, 2x) → 活力评估算例
4  反复算：ask "22°C 下主发酵要多久" → 算例①；改水温再 ask → 算例②，
   算例①标 stale（依赖的输入变了）。**先验算例挂在 Recipe 平面**
5  mix → 双向缓冲；start(type=autolyse 水合静置)
6  把头伸进料理机：intervene(mixing) + observe("面筋到窗口膜 70%") →
   resume。若干预改了参数：set(厨师机, 速度, 2) + intervene 记录原因
7  折叠 ×4：4 个 start(type=fold)+end 事件嵌在主发酵区间内（脊柱上
   的 ◆ 嵌套符号）
8  主发酵：start(type=bulk, 开放结束) → 隔夜 → 第二天 end
9  反事实："如果当时水温 30°C 会怎样" → 破坏性/反事实 → Analysis
   沙盒（浅拷贝：引用基线 Run + overlay 差异，不碰原事件）
10 record(主发酵, 实际 14h) measured → 锁点 → 算例①②全部标过期
11 fork Recipe "高水合版"（水 400g）；diff 两版本：水合率变量不同
12 start(type=bake, 铸铁锅) → end → record → export
```

**不用 CakeCAD：** 酸包是烘焙笔记的重灾区：酵种喂养历史断代（上周喂的什么比例？）；发酵时长×温度换算靠 app+Excel 两头跑；"上次那个 78% 水合的更好吃"是哪个版本找不回；过程干预（开盖看了三次）无记录；复现失败无法归因（酵种弱了还是室温高了？两个变量都变了）。

**本 bench 暴露（最重，全部指向 CAE）：** 算例采纳、反事实拷贝深度、算例与 Recipe 版本的绑定、算例链式依赖——见 §14。

---

## 12. 从四个 bench 归纳的原子操作全集

把四个流程里用户真实做过的动作去重，得到原子操作清单（"用户视角的动作"，不是内核事件）：

| # | 原子操作 | 出现在 | 现有命令覆盖 | 备注 |
|---|---|---|---|---|
| a1 | 引入对象（从库拖出/新建） | A B C D | new | |
| a2 | 摆位（不动账） | A B C | place | |
| a3 | 称重/测量（产生锁点） | A B C D | measure | |
| a4 | 转移物料（动账） | A B C D | pour / takeout | 方向不同，同内核 |
| a5 | 开/关过程区间 | A B C D | start / end（mix 为 combine 特化） | |
| a6 | 过程中介入/暂停/恢复/中止 | A B C D | intervene / pause / resume / abort | D6 待拍 |
| a7 | 观察记录（无承诺信息） | A C D | observe/mark | |
| a8 | 命名/改名 | A B C D | name | |
| a9 | 分件（等分/按部件） | A B C D | split（+part 参数） | O2/O3/继承规则待定 |
| a10 | 调用能力声明（能切/能塞/能劈） | B C | **无专属命令** | 走通用动词+部件宾语，还是独立"能力菜单"？见 §13 |
| a11 | 绑定/解绑来源 | C D | bind / unbind | D6 待拍；unbind 四 bench 未出现，但纠错必需 |
| a12 | 设定参数（炉温/机速） | A C D | set | D6 待拍 |
| a13 | 提问→算例→结果卡 | A B C D | ask | |
| a14 | 回填实测（锁点+下游标过期） | A B C D | record → 并入 measure（D1） | |
| a15 | **采纳算例结果为计划值** | D（隐含） | **无命令** | CAE 新缺口，见 §14-G1 |
| a16 | Recipe 分叉 | D | fork | |
| a17 | 召唤泳道（只读对照） | 对比场景 | lanes（UI 动作层） | |
| a18 | 导出手册 | A B C D | export（UI 动作层） | |
| a19 | 定义方法/能力包存库 | C | defmethod（+能力包同构？O1） | |
| a20 | 撤销/重做 | 全部 | undo / redo（D9） | |
| a21 | 时间操作（移 playhead/拖区间边界） | A B D | UI 动作层（非命令） | 时间脊柱直接拖拽 |

四个 bench 没有冒出清单外的新动作——命令体系的覆盖面在实战中成立。真正的缺口只有一个：**a15 采纳**（算例结果怎么变成计划事实）。

## 13. 保留到最后的命令集（草案）

筛选标准：①四个 bench 至少一处不可替代地使用；②无法由其它命令零成本组合；③心智模型独立（用户会把它当一个动词想）。

```text
Run 平面（事实写入，12 个）：
  new · name · place · pour · takeout · mix · split · start · end
  measure（并吞 record，宾语=对象/过程/Run）
  intervene · observe
Run 平面（过程控制，4 个，低频但不可替代）：
  pause · resume · abort · set
绑定类（2 个）：bind · unbind
Recipe 平面（1）：fork
注册表平面（1）：defmethod（若 O1 判定能力包同构，则它兼管能力包存库）
Analysis 平面（2）：ask · adopt（新增：采纳算例结果为 declared/planned 值）
meta（2）：undo · redo
UI 动作层（不入注册表）：lanes · export · 时间拖拽 · 书签 · 档案 · 热区
```

共 **24 个注册命令 + 6 类 UI 动作**。与 D1–D11 草案的差异：新增 adopt（bench D 逼出来的）；mark 并入 observe（带标签参数）；清空/装配维持不补（别名/MethodDef）。

**a10（调用能力声明）的裁决草案**：不需要专属命令。能力声明渲染为右键菜单里的谓语（右键火鸡→"掰下部件…"→选右腿），执行时仍走 split/takeout 等通用内核——能力声明是**菜单生成器+后果模板**，不是新动词。这保住了"注册表小、能力库无限生长"的结构。

## 14. CAE / 懒仿真：bench 逼出的未定项

用户的判断正确："还有一大堆没定"。以下每项标注已有拍板覆盖到哪、真开放的是什么：

**G1 算例采纳（adopt）——真缺口。** 算例结果不能自动成为事实（已拍），但 bench D 里用户算出"发酵 14h"后说"就按这个排计划"——这是把 Speculative 级别的算例输出变成 Recipe 里的 planned 值。草案：新命令 adopt，写入值带 source=算例引用+置信级，可被实测覆盖；采纳不是提升置信，只是登记意图。

**G2 先验/后验/反事实落在哪个平面——可推导，需确认。** 草案：先验算例引用 Recipe（计划中），后验引用 Run（已发生），反事实引用任一基线但只能在 Analysis 沙盒运行。ask 命令自动判定平面（看宾语），不要求用户选。

**G3 反事实沙盒的拷贝深度——好消息，append-only 已给出答案。** 事件日志不可变 → 浅拷贝天然成立：沙盒=基线引用+overlay 差异事件，不复制全量状态。深拷贝只在"基线要被删"时才需要，而 Run 永不删。草案：一律浅拷贝，禁止深拷贝（成本+一致性双收益）。

**G4 算例与 Recipe 版本的绑定（sync 问题）——真开放。** fork 出"高水合版"后，旧版本上的算例对新版本算不算数？草案：算例绑定创建时的版本快照（同外部引用锁版本原则）；新版本不自动继承算例，但可列出"可能仍适用的旧算例"供手动重新引用；绝不静默联动。

**G5 算例链式依赖——真开放。** 算例 A 的输出喂算例 B（发酵活力评估→主发酵时长）：置信级联怎么算？草案：允许链式，但 Case Preview 必须显示上游算例的置信级，级联取最低级；链长不限制。

**G6 快速计算要不要过 Case Preview 闸门——真开放。** 五分类里"快速计算"（水合率心算、单位换算）如果也要过完整闸门，闸门就杀死了它的价值。草案：分级闸门——快速计算只显示一行"用了什么"，完整算例才显示四段 Preview。

**G7 模型胶囊库与能力包的同构性——O1 的亲戚。** 官方传热经验公式和用户自造"我的烤箱偏高 10°C"修正项，是否同一注册机制？草案：同构（都是带来源+适用域的注册表数据），与能力包/MethodDef 共用"注册表数据而非代码"原则。

**G8 谁触发重算——已基本定，确认即可。** 懒仿真=不提问不算；实测回填自动标 stale 但不自动重算；重算永远由用户再次 ask 触发。

**已拍板覆盖、不再开放的**：分析不自动成为事实（除 adopt 外）；破坏性进沙盒；Unknown 合法；五级置信；确定性计算；Agent 只生成算例不生成结论；区间而非伪精度。

## 15. 下一步裁决顺序建议

1. **D1–D11**（命令层，每条已有草案+理由，可批量过）；
2. **§13 终版命令集**（含新增 adopt、mark 并入 observe、能力声明=菜单生成器不占动词）；
3. **G1–G8**（CAE 未定项，每条已有草案）；
4. **O1–O5**（能力声明细项，其中 O2/O3 被 bench B、C 验证为高频，优先级提升）；
5. 全部过完后一次性回写 MASTER_SPEC B.4 / C.13 / D 章，命令与 CAE 体系闭环。
